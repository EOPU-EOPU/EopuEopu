# 물고기 협동 멀티 — 프로젝트 규칙

3D 강 물고기 시점, 4인 협동. Unity 6000.0.83f1 / URP / Mirror.
개발 6주. 팀 4명 전원 클라이언트 프로그래머 (네트워크 담당 1 + 게임 기능 3).

---

## ▶ 작업을 시작하기 전에

```
1. STATUS.md 를 읽는다            ← 지금 어느 페이즈인지, 다음 작업이 뭔지
2. Docs/ROADMAP.md 의 해당 페이즈 절만 읽는다
3. 작업한다
4. STATUS.md 를 갱신한다
```

**앞에서부터 문서를 읽지 않는다.** 지금 작업에 해당하는 곳만 연다.

---

## 문서 지도

| 알고 싶은 것 | 파일 |
|---|---|
| **지금 뭘 해야 하나** | `STATUS.md` |
| **어떤 순서로 만드나** | `Docs/ROADMAP.md` |
| **무엇을 만드나 · 누가 정하나** | `Docs/SPEC.md` |
| 플레이어 상태(HFSM)를 어떻게 짜나 | `Docs/PLAYER.md` |
| 이동을 어떻게 만드나 | `Docs/MOTION.md` |
| 팀원이 뭘 상속하나 | `Docs/API.md` |
| 테스트 절차 | `Docs/TESTING.md` |
| 멀티플레이가 처음이다 | `Docs/LEARN/01_CONCEPTS.md` |
| 왜 내 물고기는 Dynamic, 남의 물고기는 Kinematic인가 | `Docs/LEARN/02_KINEMATIC.md` |
| 후순위 기능을 착수할 때만 | `Docs/BACKLOG.md` |

각 스크립트 폴더에 `CLAUDE.md`가 따로 있다. **작업 중인 폴더의 CLAUDE.md가 이 파일보다 우선한다.**

---

## 폴더 = 소관

| 경로 | 담당 | Mirror 직접 사용 |
|---|---|---|
| `Assets/01_Scripts/_Net/` | 네트워크 담당 1명 | 허용 |
| `Assets/01_Scripts/_Game/` | 게임 기능 담당 | **금지** — 베이스 3종 상속 + `NetPlayer` 공개 멤버만. **`PlayerMotor`·HFSM도 여기다** |
| `Assets/01_Scripts/_Local/` | UI·연출·카메라 담당 | **금지** — 컴파일러가 차단 |
| `Assets/01_Scripts/_Contracts/` | 네트워크 담당 (경계 정의) | **없음** — 순수 C# 계약 |

`_Local`은 asmdef에 Mirror 참조가 없어서 **컴파일러가 막는다.**
`_Game`은 베이스 상속 때문에 Mirror 참조가 필요하므로 **컴파일러가 못 막는다. 규칙과 리뷰로 막는다.**

> `_Contracts` 분리는 **P07 작업 항목**이다. 현재 `_Local.asmdef`은 아직 `FishCoop.Net`을 참조한다.
> 분리가 끝나면 `FishCoop.Contracts`로 바꾼다.
> **어느 쪽이든 `_Local/`에서 Mirror 기반 타입을 쓰지 않는 규칙은 같다.**

---

## 베이스 클래스는 3개다

팀원 3명이 상속하는 표면은 이것뿐이다. 시그니처는 `Docs/API.md`가 정본이다.

| 베이스 | 쓰임 | 구현 개수 |
|---|---|---|
| `NetServerAI` | 포식자 · 피식자 | 2+ |
| `NetObjective` | 정화 퀘스트 | 여러 개 |
| `NetAbility` | 물고기 4종 능력 | 4 |

> **플레이어 이동은 베이스가 아니다.** `_Game/PlayerMotor`(일반 MonoBehaviour) + Mirror `NetworkTransformReliable`.
> `NetPlayerMotor`는 2026-09-23에 폐기했다.
>
> **`NetInteractable`은 지금 만들지 않는다.** 용례가 "피식자 먹기" 하나뿐이라
> `NetPlayer.RequestEat`로 처리한다. 자원 채집이 후순위에서 돌아와 용례가 2개가 되면 그때 추출한다.
>
> **새 추상화는 게임 기능 2개 이상이 같은 걸 필요로 할 때만 만든다.**
> 프레임워크를 키우다 게임이 안 나오는 게 이 프로젝트 최대 위험이다.

---

## 전역 금지

| 쓰지 말 것 | 대신 | 왜 |
|---|---|---|
| `Instantiate()` / `Destroy()` | `NetSpawner.Spawn/Despawn` | 클라가 만든 오브젝트는 그 클라에만 있는 유령 |
| `Time.time`, `Time.realtimeSinceStartup` | `NetworkTime.time` | 4명이 서로 다른 값을 본다 |
| `static`에 게임 상태(수치·진행도) 보관 | SyncVar / 베이스 상태 | 4대에서 각자 다른 값이 된다. 조회용 static(`NetPlayer.Local`)은 예외 |
| `FindObjectOfType<NetPlayer>()` | `NetPlayer.Local` / `NetPlayer.Find(id)` | 남의 플레이어가 잡힌다 |
| 원격 복제본 · AI를 Dynamic Rigidbody로 두기 | **소유자만 Dynamic**, 나머지는 kinematic (프리팹 기본값 kinematic) | 원격에서 물리와 NT가 Transform을 서로 잡아당겨 떤다 |
| `OnCollisionEnter` 등 클라 물리 충돌로 게임 판정 | 서버 판정 | 4대 PC에서 각자 발생한다. 4명이 다른 결과를 본다 |
| 요청에 좌표를 실어 보내 판정 | 대상 ID만 보내고 **서버가 가진 Transform + 허용오차** | 판정 주체는 서버다. 클라 좌표를 요청에서 받기 시작하면 판정이 클라로 넘어간다 |
| 서버가 플레이어 Transform을 직접 쓰기 | 소유자에게 `Respawn` / `ApplyImpulse` 요청 | 소유자가 보내는 다음 스냅샷에 덮인다 |
| 원격 복제본에서 `PlayerMotor` 실행 | NT 보간 표시 | 남의 캐릭터를 내 PC가 다시 계산하면 NT와 어긋나 떨린다 |
| `NetworkAnimator` | `NetPlayer.State` SyncVar → `_Local`에서 구동 | 파라미터를 계속 보내고, 트리거는 Late Join에 복원되지 않는다. 필요해지면 그때 결정 |
| 판정용 난수를 클라에서 굴리기 | 서버가 굴리고 결과만 전파 | 4명이 다른 결과를 본다 |

---

## 되돌리지 말 것

### 기술 결정

- **Mirror 확정.** FishNet 등 다른 라이브러리로 전환하지 않는다. **제안도 하지 말 것.**
- **호스트 모드.** 전용 서버 빌드는 범위 밖.
- **이동만 클라 권위, 나머지는 전부 서버 권위.** (2026-09-23 결정. 09-18의 서버 권위 + 예측·재조정을 되돌렸다.
  예측·재조정을 다시 제안하지 않는다. 옛 설계는 `Docs/archive/2026-09-18_server-auth-motion.md`)
- **이동 계산은 `_Game/PlayerMotor` 한 곳에만.** 소유 클라에서만 돌고, `NetworkTransformReliable`(ClientToServer)이 나른다.
- **소유자 물고기만 Dynamic Rigidbody, 원격 복제본 · AI는 kinematic.** (2026-09-23. 지형 충돌을 물리에 맡긴다.
  플레이어↔플레이어 · 플레이어↔AI 물리 충돌은 끈다. 규칙은 `Docs/MOTION.md` 1.1절)
- **트랜스포트는 `MultiplexTransport`** (현재 KCP만 등록). Steam은 3주차 이후 예정이므로 벗겨내지 말 것.
  단, Multiplex가 KCP와 Steam을 알아서 골라 준다고 가정하지 않는다.

### P02 게이트

**클라 권위 이동(소유 클라 이동 · NT 원격 보간 · 서버 판정)은 4인 원격 테스트의 진입 조건이다.**

```
T16 즉시 반응  내 조작이 즉시 반응하는가
T18 보간       남의 물고기가 안 떨리는가
T19 서버판정   판정이 서버의 Transform + 허용오차로 이루어지는가
```

**반드시 원격 Client에서 판정한다.** 호스트는 서버와 같은 프로세스라 지연이 0이어서
전부 저절로 통과한 것처럼 보인다.

통과 전에는 다인 테스트를 열지 않는다. T17(재조정)은 삭제됐다.

→ `Docs/ROADMAP.md` P02

### P02에서 미리 박는 이동 계약

**지금 `.cs`가 0개라 공짜다. 나중에 넣으면 팀원 코드까지 깨진다.**

전문은 **`Docs/MOTION.md` 한 곳에만** 있다. 여기서는 *왜 필요한지*만 적는다.

| 넣어야 할 것 | 빼먹으면 |
|---|---|
| 서버 → 소유자 **`ApplyImpulse`** 통로 | 넉백·후순위 낚시바늘을 넣을 때 Motor 구조를 다시 뜯어야 한다 |
| 서버 → 소유자 **`Respawn`** + NT 버퍼 초기화 | 부활 직후 죽은 자리로 돌아가거나, 남의 화면에서 맵을 가로질러 미끄러진다 |
| **`IsDowned` · `SlowStage`** 를 Motor가 읽는 자리 | 행동 불능·감속을 상태마다 따로 넣다가 빠뜨린다 |
| **`ReportState`** — Downed는 서버가 강제 | 클라가 HP로 Downed를 판단해 서버와 어긋난다 |

→ 전문은 `Docs/MOTION.md`

### 게임 규칙

- **HP·허기·오염도·구역·성장·능력은 전부 서버 권위다.**
- **허기는 0~10 정수 단계다.** 연속값을 동기화하지 않는다. 회복 수단은 피식자 포식 하나뿐이다.
- **HP 0 → 행동 불능 → 15초 자동 부활.** 실패 조건은 없다.
- **능력은 서버 판정이고, "없으면 못 함"이 아니라 "있으면 빠름"으로 설계한다.**
- **HUD가 필요한 값은 전부 `_Contracts` 계약을 통해 `_Local/`에 간다.**
- **임계값·보간 지연·전송 주기는 측정 전 시작값이다.** 조정하고 `STATUS.md`에 남긴다.
  **계약 자체는 조정 대상이 아니다.**

→ 기능 명세 전문은 `Docs/SPEC.md`

---

## 범위

```
만든다   클라 권위 이동(NT) · 포식(허기+성장) · 포식자/피식자 AI
         HP · 행동 불능 · 15초 자동 부활
         정화 · 오염도 · 승리 · 구역 표시 · 능력 4종

후순위   새 · 자원/인벤토리/창고 · 건설/부화/기지 · 낚시바늘   → Docs/BACKLOG.md
제외     동료 구조 · 전원 사망 재시작
범위 밖  호스트 마이그레이션 · 재접속 복구 · 세이브/로드 · 전용 서버
         완전한 안티치트 · 지연 보상 · 5인 이상
         초대 코드/무작위 매칭 · 구역별 버프
```

> **클라 예측·재조정은 만들지 않는다**(2026-09-23). 이동이 클라 권위라 필요 없다.

**낚시바늘은 핵심 기획이지만 이동을 건드린다.**
그래서 P02에서 서버 → 소유자 `ApplyImpulse` 통로를 미리 넣는다.

→ 경계 전문과 기획서 대조는 `Docs/SPEC.md` 5절 · 7절

---

## 문서를 고칠 때

**규범은 한 곳에만 쓴다.** 이게 이 문서 세트의 유일한 설계 원칙이다.

| 바뀌는 것 | 고칠 곳 |
|---|---|
| 기능 범위 · 권위 | `Docs/SPEC.md` → 그 다음 `ROADMAP.md` 페이즈 조정 |
| 이동 계약 · 수치 | `Docs/MOTION.md` |
| 베이스 시그니처 | `Docs/API.md` + 실제 구현을 **같은 커밋에서** |
| 진행 상태 · 측정값 | `STATUS.md` |
| 뺀 기능의 설계 | 지우지 말고 `Docs/BACKLOG.md`로 옮긴다 |

**다른 문서에서는 복사하지 말고 링크한다.** 복사하면 결정이 바뀔 때 반드시 하나를 빠뜨리고,
그때부터 문서끼리 다른 말을 한다.

---

**기능을 늘리자는 제안을 하지 말 것.** 네트워크 담당 1명이 병목이고, 기능이 늘면 그 1명에게 전부 몰린다.
남는 여력은 완성도(아트·연출·UI·사운드·레벨)에 쓴다.
